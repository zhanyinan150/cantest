# 蓝安的代码内容

> **最后更新**: 2026-08-01 (MotorAutoTask 改为全流程Y轴位移提取测试, 13步只动Y)
>
> **规则**: 每次代码修改后必须同步更新本文档。

---

## 一、项目概况

STM32F4xx + FreeRTOS 的 XYZ 起重机分拣机器人，配合 K230 视觉模块完成"抓豆子->识别数字->放箱子"的比赛流程。

- **MCU**: STM32F407 (Cortex-M4F)
- **RTOS**: FreeRTOS (CMSIS-RTOS V2)
- **IDE**: Keil MDK-ARM (ARMCC V5.06, 需勾选 MicroLIB)
- **通信**: CAN1(M2006 升降) / CAN2(Y轴双步进) / UART5(X轴步进) / UART4(舵机) / USART1(日志) / USART3(K230视觉)
- **分层**: App -> Modules -> BSP -> Core

---

## 二、项目文件结构

### App 层

| 文件 | 说明 |
|------|------|
| [app/app_init.c](app/app_init.c) | 系统初始化编排 + MotorAutoTask 上电动作任务 |
| [app/app_init.h](app/app_init.h) | App_Init 声明 |

### Modules 层

| 文件 | 说明 |
|------|------|
| [modules/action/action.c](modules/action/action.c) | 动作序列(单爪抓豆子->放箱子) + K230 三阶段通讯**调用** |
| [modules/action/action.h](modules/action/action.h) | Action_Init / goto_box 声明 |
| [modules/k230/K230.c](modules/k230/K230.c) | K230 视觉通信 + 三阶段协议层(发命令->等ACK->等数据->回ACK) |
| [modules/k230/K230.h](modules/k230/K230.h) | K230 接口声明 + 命令码/颜色/超时宏 |
| [modules/motor/motor.c](modules/motor/motor.c) | XYZ 三轴联动 (X:UART5, Y:CAN2双同步, Z:lift闭环) |
| [modules/motor/motor.h](modules/motor/motor.h) | Motor_Init / Motor_XYZ 声明 + 电气地址/运动参数 |
| [modules/lift/lift.c](modules/lift/lift.c) | Z轴升降 (M2006 + 串级PID闭环, LiftTask 20ms) |
| [modules/lift/lift.h](modules/lift/lift.h) | Lift_Init/Up/Down/MoveTo/Stop + PID调参API |
| [modules/mech_params.h](modules/mech_params.h) | 集中机械参数(轮径/周长/减速比/每转脉冲) |
| [modules/servo/servo.c](modules/servo/servo.c) | LSC舵机控制板驱动(UART4, runActionGroup等) |
| [modules/servo/servo.h](modules/servo/servo.h) | 舵机接口声明 |
| [modules/duoji/LobotServoController.c](modules/duoji/LobotServoController.c) | 原始舵机驱动(UART6, 未使用的参考代码) |
| [modules/duoji/LobotServoController.h](modules/duoji/LobotServoController.h) | 原始舵机接口声明 |
| [modules/dji_motor/dji_motor.c](modules/dji_motor/dji_motor.c) | DJI智能电机驱动(M2006/C610, 串级PID, 守护进程) |
| [modules/dji_motor/dji_motor.h](modules/dji_motor/dji_motor.h) | DJIMotorInit/SetRef/Enable/Stop 声明 |
| [modules/dji_motor/motor_def.h](modules/dji_motor/motor_def.h) | 电机通用枚举/结构体(闭环类型/反馈源/PID配置) |
| [modules/PID/PID.c](modules/PID/PID.c) | PID 控制器实现(变积分/梯形积分/微分滤波/S曲线) |
| [modules/PID/PID.h](modules/PID/PID.h) | PID 接口声明 + 改进项枚举 |
| [modules/ADRC/ADRC.c](modules/ADRC/ADRC.c) | 自抗扰控制器(TD+ESO+NLSEF, 当前未使用) |
| [modules/ADRC/ADRC.h](modules/ADRC/ADRC.h) | ADRC 接口声明 |
| [modules/ZDT_EmmV5/Emm_V5.c](modules/ZDT_EmmV5/Emm_V5.c) | Emm_V5 UART版步进驱动(UART5, X轴用) |
| [modules/ZDT_EmmV5/Emm_V5.h](modules/ZDT_EmmV5/Emm_V5.h) | UART版步进接口声明 |
| [modules/ZDT_EmmV5/CAN/Emm_V5_CAN.c](modules/ZDT_EmmV5/CAN/Emm_V5_CAN.c) | Emm_V5 CAN版步进驱动(CAN2, Y轴双电机用) |
| [modules/ZDT_EmmV5/CAN/Emm_V5_CAN.h](modules/ZDT_EmmV5/CAN/Emm_V5_CAN.h) | CAN版步进接口声明 + 状态标志位宏 |
| [modules/ZDT_X42/ZDT_X42_V2.c](modules/ZDT_X42/ZDT_X42_V2.c) | ZDT X42第二代步进驱动(当前未使用) |
| [modules/ZDT_X42/ZDT_X42_V2.h](modules/ZDT_X42/ZDT_X42_V2.h) | X42接口声明 |

### Core 层 (CubeMX 生成)

| 文件 | 说明 |
|------|------|
| Core/Src/main.c | 入口, SystemClock_Config + MX_*_Init |
| Core/Src/can.c | CAN1/CAN2 初始化 |
| Core/Src/usart.c | USART1/UART4/UART5/USART3 初始化 |
| Core/Src/freertos.c | StartDefaultTask -> App_Init() |
| Core/Src/gpio.c, dma.c, crc.c | GPIO/DMA/CRC 初始化 |

---

## 三、系统架构与调用关系

### 3.1 初始化流程 (App_Init)

```
App_Init()                                    [app/app_init.c]
  ├── BSPLogInit()                            日志队列 + LogTask (DMA 发 USART1)
  ├── Lift_Init()                             M2006 注册(CAN1) + LiftTask(20ms) + DJIMotorTask(10ms)
  ├── HAL_CAN_Start(&hcan1)                   CAN1 启动 + 接收中断
  ├── Emm_V5_CAN_Init({1,2}, 2)              Y1/Y2 注册(CAN2 扩展帧)
  ├── HAL_CAN_Start(&hcan2)                   CAN2 启动 + 接收中断
  ├── Motor_Init()                            X(UART5) + Y1/Y2(CAN2) 步进使能
  ├── UART_Callback_Init()                    USART1 命令接收 + 回调分发
  ├── K230_Init()                             USART3 回调注册 + DMA 接收启动
  └── osThreadNew(MotorAutoTask, ...)         上电自动动作任务
      (Action_Init() 已注释, 当前用 MotorAutoTask)
```

### 3.2 任务列表

| 任务名 | 优先级 | 栈大小(word) | 周期 | 文件 |
|--------|--------|-------------|------|------|
| DJIMotorTask | osPriorityHigh(40) | 512 | 10ms | dji_motor.c |
| LiftTask | osPriorityAboveNormal(32) | 512 | 20ms | lift.c |
| MotorAutoTask | osPriorityNormal(24) | 1024 | 一次性 | app_init.c |
| ActionTask | osPriorityNormal(24) | 1024 | 一次性 | action.c (当前注释) |
| LogTask | 低 | - | 队列驱动 | bsp_log.c |
| CommandTask | - | - | 队列驱动 | bsp/cmd |

### 3.3 通信总线分配

| 总线 | 外设 | 用途 | 波特率 |
|------|------|------|--------|
| CAN1 | hcan1 | M2006 升降电机反馈 (标准帧 0x200+ID) | 1Mbps |
| CAN2 | hcan2 | Y1/Y2 步进电机控制+反馈 (扩展帧 Addr<<8) | 1Mbps |
| UART5 | huart5 | X轴步进电机控制 (Emm_V5 UART版) | 115200 |
| UART4 | huart4 | LSC舵机控制板 | 9600 |
| USART1 | huart1 | 日志输出 (DMA) + VOFA 调参命令 | 115200 |
| USART3 | huart3 | K230 视觉模块通信 (DMA 收发) | 115200 |

---

## 四、核心模块详解

### 4.1 action.c - 动作序列 (单爪结构)

#### 结构概述

单爪结构: 只用黑爪, 分3次抓豆(左->中->右), 每次抓完去箱子放豆。
- 只有第1次抓豆前做 Phase1(豆子识别)
- 只有第1次放箱时做 Phase2/Phase3(箱子识别)
- 左/右豆: 组15(准备) -> Z降 -> 组16(抓) -> Z升
- 中豆: 组3(准备) -> Z降 -> 组4(抓) -> Z升
- 每次抓完豆后 -> 组12(过渡) -> 走到右豆位置(X=-57.5) -> 去放箱子

#### 豆子X轴坐标 (以初始点为原点, 靠墙dir=1为正)

```
左豆: +35 (起始dir=1靠墙走35)
中豆: +14.5 (从左豆dir=0远离墙走20.5)
右豆: -57.5 (从中豆dir=0远离墙走24)

左豆 -> 中豆: dir=0, 20.5cm
中豆 -> 右豆: dir=0, 24cm
```

#### "每次抓完走到右豆"的X移动

```
左豆(+35) -> 右豆(-57.5): dir=0, 92.5cm  [action_grab_left]
中豆(+14.5) -> 右豆(-57.5): dir=0, 24cm  [action_grab_middle]
右豆: 已在位, 无需移动                [action_grab_right]
右豆(-57.5) -> 4号箱(+24.5): dir=1, 82cm  [action_gouto_xiangzi_first/secend]
```

#### 函数清单

| 函数 | 可见性 | 功能 |
|------|--------|------|
| `Action_Init()` | extern | 创建 action_all 任务 |
| `action_all()` | static | 完整3次抓放流程: wait_ready->Phase1->抓左豆->放箱(含视觉)->抓中豆->放箱->抓右豆->放箱 |
| `action_grab_left()` | static | 第1次抓豆(左边): X+35->组15->Z降->组16->Z升->组12->X-92.5到右豆 |
| `action_grab_middle()` | static | 第2次抓豆(中间): 从箱回豆区->X-82到右豆->X+24到中豆->组3->Z降->组4->Z升->组12->X-24回右豆 |
| `action_grab_right()` | static | 第3次抓豆(右边): 从箱回豆区->X-82->组15->Z降->组16->Z升->组12(已在右豆,直接去放箱) |
| `action_gouto_xiangzi_first()` | static | 第1次放箱(含Phase2/Phase3视觉识别) |
| `action_gouto_xiangzi_secend()` | static | 第2/3次放箱(无视觉) |
| `goto_box(target_box)` | extern | 箱子间X轴移动 + 完整放料流程(仅黑爪: 准备+Z降+组18开爪+Z升) |
| `goto_box_ready(target_box)` | extern | 箱子间X轴移动 + 组15定位(不做放料): move_box(3)->组15->move_box(target) |
| `move_box(target_box)` | extern | 只移动X轴到目标箱子, 不执行放料 |
| `dbg(s)` | static | 调试输出: printf->日志队列->USART1 |

#### 方向定义

```
Y轴: dir=1(前/去豆子), dir=0(后/去箱子)
X轴: dir=0(左/远离墙), dir=1(右/靠近墙)
Z轴: dir=0(上),        dir=1(下)
```

#### action_all() 流程

```
k230_wait_ready()
k230_phase1_bean()                    // 仅第1次抓豆前识别豆子
action_grab_left()                    // 抓左边豆子
action_gouto_xiangzi_first()          // 放箱(含Phase2/Phase3)
goto_box(Data_yinshe(bean_color[0]))  // 放左边豆子
move_box(4)                           // 回4号箱中转
action_grab_middle()                  // 抓中间豆子
action_gouto_xiangzi_secend()         // 放箱(无视觉)
goto_box(Data_yinshe(bean_color[1]))  // 放中间豆子
move_box(4)                           // 回4号箱中转
action_grab_right()                   // 抓右边豆子
action_gouto_xiangzi_secend()         // 放箱(无视觉)
goto_box(Data_yinshe(bean_color[2]))  // 放右边豆子
```

#### 舵机动作组一览

| 动作组 | 用途 |
|--------|------|
| 组3 | 黑爪准备抓中间豆子(转轴) |
| 组4 | 黑爪抓中间豆子 |
| 组7 | 黑爪准备放箱子1 |
| 组9 | 黑爪准备放箱子5 |
| 组12 | 黑爪准备放中间箱子(2-4) / 抓豆后过渡 |
| 组15 | 黑爪准备抓左边或右边豆子 |
| 组16 | 黑爪抓左边或右边豆子 |
| 组18 | 黑爪开爪放豆子 |

#### goto_box 函数 (单爪-黑爪)

```c
void goto_box(uint8_t target_box);
// target_box: 目标箱子 1~5
```

完整放料流程: move_box(3)中转 -> 准备动作组 -> move_box(target_box) -> Z下降22cm -> 组18开爪 -> Z上升22cm

**准备动作组** (仅黑爪):

| 箱子1 | 箱子2-4(中间) | 箱子5 |
|-------|--------------|-------|
| 组7 | 组12 | 组9 |

#### 箱子坐标 (以第4个箱子为原点)

```c
static const float box_x_coord[6] = {0, -93.0f, -73.0f, -35.0f, 0.0f, 25.0f};
// 箱子1: -93cm  箱子2: -73cm  箱子3: -35cm  箱子4: 0cm  箱子5: +25cm
static uint8_t s_current_box = 4;  // 当前所在箱子
```

#### action_grab_left() 动作序列

| 步骤 | 动作 | X | Y | Z | 舵机 |
|------|------|---|---|---|------|
| 1/6 | 起始->左豆(X=+35) | +35(dir=1) | +190 | +35(上) | - |
| - | 准备抓左豆 | - | - | - | runActionGroup(15,1) |
| 2/6 | 降爪抓左豆 | 0 | 0 | -25(下) | - |
| - | 黑爪抓 | - | - | - | runActionGroup(16,1) |
| 3/6 | 升爪复位 | 0 | 0 | +25(上) | - |
| 4/6 | 过渡 | - | - | - | runActionGroup(12,1) |
| 5/6 | 走到右豆位置(X=-57.5) | -92.5(dir=0) | 0 | 0 | - |

#### action_grab_middle() 动作序列

| 步骤 | 动作 | X | Y | Z | 舵机 |
|------|------|---|---|---|------|
| 1/8 | 从4号箱回豆区 | 0 | +120 | 0 | - |
| 2/8 | 到右豆位置(X=-57.5) | -82(dir=0) | +195 | 0 | - |
| 3/8 | 从右豆到中豆(X=+14.5) | +24(dir=1) | 0 | 0 | runActionGroup(3,1) |
| 4/8 | 降爪抓中豆 | 0 | 0 | -15(下) | - |
| - | 黑爪抓中豆 | - | - | - | runActionGroup(4,1) |
| 5/8 | 升爪复位 | 0 | 0 | +15(上) | - |
| 6/8 | 过渡 | - | - | - | runActionGroup(12,1) |
| 7/8 | 走到右豆位置(X=-57.5) | -24(dir=0) | 0 | 0 | - |

> 流程与 action_grab_right 一致: 先从箱子去右豆, 再根据X坐标去抓中豆
> 右豆X=-57.5, 中豆X=+14.5, 差=+24 -> dir=1(靠近墙) 24cm (右豆到中豆)
> 中豆X=+14.5, 右豆X=-57.5, 差=-24 -> dir=0(远离墙) 24cm (中豆回右豆)

#### action_grab_right() 动作序列

| 步骤 | 动作 | X | Y | Z | 舵机 |
|------|------|---|---|---|------|
| 1/7 | 从4号箱回豆区 | 0 | +120 | 0 | - |
| 2/7 | 到右豆位置(X=-57.5) | -82(dir=0) | +195 | 0 | - |
| - | 准备抓右豆 | - | - | - | runActionGroup(15,1) |
| 3/7 | 降爪抓右豆 | 0 | 0 | -20(下) | - |
| - | 黑爪抓 | - | - | - | runActionGroup(16,1) |
| 4/7 | 升爪复位 | 0 | 0 | +20(上) | - |
| 5/7 | 过渡去放箱子 | - | - | - | runActionGroup(12,1) |

> 右豆X=-57.5, 4号箱X≈+24.5, 差=-82 -> dir=0(远离墙) 82cm
> 抓完后已在右豆位置, 无需额外X移动, 直接去放箱子

#### action_gouto_xiangzi_first() 动作序列 (含视觉)

| 步骤 | 动作 | X | Y | Z |
|------|------|---|---|---|
| 障碍1 | Y-100 | 0 | -100 | 0 |
| 障碍2 | X+82 Y-158 | +82 | -158 | 0 |
| Phase2 | k230_phase2_front() 看正面数字 | - | - | - |
| 到箱子 | Y-52 | 0 | -52 | 0 |
| Phase3 | k230_phase3_side() 看侧面数字 | - | - | - |
| 微调 | Y-8.5 | 0 | -8.5 | 0 |

#### action_gouto_xiangzi_secend() 动作序列 (无视觉)

| 步骤 | 动作 | X | Y | Z |
|------|------|---|---|---|
| 障碍1 | Y-100 | 0 | -100 | 0 |
| 障碍2 | X+82 Y-215 | +82 | -215 | 0 |

> **注**: 原代码中 action_gouto_xiangzi_secend 含 runActionGroup(12,1), 现已移至各抓豆函数末尾。
> X+82 = 右豆(-57.5)到4号箱(+24.5)的距离82cm。

---

### 4.2 K230.c - 视觉通信 + 协议层

#### 通信协议

- **帧格式**: 8字节, [0]=帧类型, [1..6]=数据, [7]=XOR校验
- **命令码**: 2=看豆, 3=看正面数字, 1=看侧面数字, 6=关闭/应答
- **帧类型**: 0x02=豆子颜色, 0x03=正面数字, 0x01=完整5数字, 0x0A=ACK

#### 模块变量

| 变量 | 类型 | 说明 |
|------|------|------|
| `bean_color[3]` | __IO uint8_t | 三颗豆子颜色(0x06绿/0x07芸/0x08黄) |
| `number_position[5]` | __IO uint8_t | 五个箱子的数字编号(1=黄/2=绿/3=芸) |
| `bean_flag` | __IO uint8_t | 豆子数据就绪(1=有新数据) |
| `bean_locked` | __IO uint8_t | bean_color 锁(1=锁住) |
| `front_number[3]` | __IO uint8_t | 正面3个数字 |
| `front_number_flag` | __IO uint8_t | 正面数字就绪 |
| `full_number_flag` | __IO uint8_t | 完整5数字就绪 |
| `k230_ack_flag` | __IO uint8_t | ACK计数器(非布尔, 避免连发ACK丢帧) |
| `count` | __IO uint8_t | 数字数据帧计数 |

#### 函数清单

| 函数 | 可见性 | 功能 |
|------|--------|------|
| `K230_Init()` | extern | 注册USART3回调 + 启动DMA接收 |
| `k230_read(huart, size)` | extern | USART3接收事件回调: 解析帧 + 重新武装 |
| `k230_write(command)` | extern | 发送8字节命令(含XOR校验) |
| `Data_yinshe(key)` | extern | 颜色->箱位映射, 返回1~5 |
| `k230_wait_ready()` | extern | 上电握手: 阻塞等K230就绪(0x0B), 超时30s也放行 |
| `k230_phase1_bean()` | extern | Phase1: 豆子颜色识别 |
| `k230_phase2_front()` | extern | Phase2: 正面数字识别(无数据帧, 等两个ACK) |
| `k230_phase3_side()` | extern | Phase3: 侧面数字+推理第5位 |
| `k230_dbg(s)` | static | 协议层调试输出 |
| `k230_wait_flag(flag, timeout)` | static | 等待事件标志(计数器语义) |
| `k230_send_cmd_once(cmd)` | static | 发一次命令+等一次ACK(不重试) |
| `k230_send_cmd_and_wait_ack(cmd)` | static | 发命令+等ACK, 重试K230_RETRY_MAX次 |
| `k230_send_cmd_wait_ack_wait_data(cmd, data_flag)` | static | 发命令+等ACK+等数据, 任一步超时重发 |

#### Data_yinshe 颜色->目标值映射

| 颜色 | 编码 | 目标值 |
|------|------|--------|
| 绿豆 BEAN_GREEN | 0x06 | 2 |
| 芸豆 BEAN_YUN | 0x07 | 3 |
| 黄豆 BEAN_YELLOW | 0x08 | 1 |

#### 超时/重试参数

```c
#define K230_ACK_TIMEOUT_MS      3000    /* 等对端 ACK */
#define K230_DATA_TIMEOUT_MS     15000   /* 等识别结果(含推理耗时) */
#define K230_RETRY_MAX           3       /* 重试次数, 只在一层生效 */
```

#### DMA 接收策略

使用 `HAL_UARTEx_ReceiveToIdle_DMA` (IDLE切帧) 而非定长 `Receive_DMA`:
- 定长模式掉字节后相位永久错位, XOR 永远失败
- IDLE 模式靠帧间空隙重新对齐, 掉字节后下一帧即恢复
- 关闭半满中断(`DMA_IT_HT`), 只关心 IDLE/收满事件

---

### 4.3 motor.c - XYZ 三轴联动

#### 电机配置

| 轴 | 电机 | 驱动 | 通信 | 地址 |
|----|------|------|------|------|
| X(左右) | Emm_V5 步进 | Emm_V5.c (UART版) | UART5 | 3 |
| Y(前后) | Emm_V5 步进×2 | Emm_V5_CAN.c | CAN2 扩展帧 | 1, 2 |
| Z(升降) | M2006 | dji_motor.c + lift.c | CAN1 标准帧 | 1 |

#### Y轴双电机同步

- Y1/Y2 固定 `snF=true` 预存, 广播 `Synchronous_motion` 同时触发
- Y2 方向取反 `!y_dir` (镜像安装, 见 [[y-motor-mirror-install]])
- 到位判定需 Y1 && Y2 均到位(当前轮询代码已注释, 用 osDelay 等待)

#### 机械参数 (mech_params.h)

| 参数 | X轴 | Y轴 | Z轴(M2006) |
|------|-----|-----|------------|
| 主动轮直径 | 1.2 cm | 10.0 cm | 0.032 m (3.2cm) |
| 减速比 | 1.0 | 1.0 | 36.0 (M2006自带36:1) |
| 每转脉冲 | 3200 (200步×16细分) | 3200 | 编码器8192 |

#### Motor_XYZ 当前行为

- 非阻塞: 发完命令即返回(轮询到位代码已注释), 靠 osDelay 等电机走完
- Z 轴通过 Lift_Up/Down 设目标, LiftTask(20ms) 闭环跟进
- 距离->脉冲换算: `clk = (距离 / 周长) × 每转脉冲 × 减速比`

---

### 4.4 lift.c - Z轴升降

- **电机**: M2006 (C610电调, CAN1 标准帧, ID=1)
- **闭环**: 角度环(外环) + 速度环(内环) 串级, 不使用电流环
- **任务**: LiftTask 20ms 周期 (osPriorityAboveNormal=32)
- **位移**: 相对增量模式, target_displacement 叠加; 绝对定位用 Lift_MoveTo
- **限幅**: ±400cm
- **PID 参数**: 角度环 Kp=8.6 Ki=0 Kd=0; 速度环 Kp=0.7 Ki=0.03 Kd=0.01
- **方向取反**: motor_reverse_flag=MOTOR_DIRECTION_REVERSE (在 dji_motor 层处理)

---

### 4.5 servo.c - 舵机控制

- **控制板**: LSC系列舵机控制板 (深圳乐幻索尔)
- **通信**: UART4, 9600 波特率
- **关键函数**: `runActionGroup(numOfAction, Times)` - 运行指定动作组
- **超时**: SERVO_TX_TIMEOUT_MS = 300ms (原值0xFFFF=65秒会卡死)
- **发送后**: osDelay(400) 确保数据发送完成

---

### 4.6 dji_motor.c - DJI 智能电机

- **支持**: M2006 / M3508 / GM6020
- **任务**: DJIMotorTask 10ms 周期 (osPriorityHigh=40)
- **守护进程**: 20ms 未收到反馈 -> 停止电机输出 + 警告
- **发送分组**: 6组(2CAN×3Group), 每组最多4电机共用一帧
- **多圈角度**: total_angle = 圈数×360 + 单圈角度, 避免边界突跃
- **临界区**: 读取 measure 时 taskENTER_CRITICAL 防止中断写与任务读混叠

---

### 4.7 PID.c - PID 控制器

- **固定周期**: dt = 0.01f (10ms, 与 DJIMotorTask 一致)
- **改进项**: 积分限幅 / 变积分 / 梯形积分 / 微分滤波 / S曲线加速度
- **积分修复**: 原实现"先矩形法 ITerm += 再用梯形值覆盖"导致积分失效, 已改为先算增量再累加

---

### 4.8 Emm_V5_CAN.c - CAN版步进驱动

- **CAN2 扩展帧**: ExtId = (Addr<<8) | Packet
- **分包**: payload>8字节时拆包, 每包首字节为功能码
- **FindByAddr**: 必须同时匹配扩展帧标志, 避免与M2006标准帧地址撞车
- **广播地址0**: 走CAN2第一个扩展帧实例(Y1), 不能用 can_instance[0](M2006)
- **Read_Flag**: 0x3A 命令读状态, 100ms超时, &0x02=到位 &0x04=堵转
- **Read_Encoder**: 0x31 命令读编码器, 累计值含回绕处理

---

### 4.9 Emm_V5.c - UART版步进驱动

- **UART5 (huart5)**: 供X轴步进(ID=3)使用
- **Emm_V5_SendCmd**: 统一发送函数, 检查返回值 + AbortTransmit 复位 gState
- **osDelay(20)**: 发送后等DMA完成 + TC中断复位 gState
- **底盘运动函数**: Motor_VelStraight/TurnBack/TurnLeft等(当前未使用)

---

## 五、app_init.c - 当前运行状态

### 5.1 App_Init 启用项

| 模块 | 状态 | 说明 |
|------|------|------|
| BSPLogInit | ✅ 启用 | 日志队列 + LogTask |
| Lift_Init | ✅ 启用 | M2006 升降 + CAN1 |
| Emm_V5_CAN_Init | ✅ 启用 | Y1/Y2 步进 + CAN2 |
| Motor_Init | ✅ 启用 | X/Y/Z 三轴使能 |
| UART_Callback_Init | ✅ 启用 | USART1 命令接收 |
| K230_Init | ✅ 启用 | K230 视觉 USART3 |
| Action_Init | ❌ 注释 | action_all 任务未启动 |
| MotorAutoTask | ✅ 启用 | 上电自动动作(全流程Y轴位移测试) |

### 5.2 MotorAutoTask 当前动作

全流程 Y 轴位移提取, 只动 Y (X=0, Z=0), 共 13 步:

| # | 来源 | Y方向 | Y距离 | delay(ms) |
|---|------|--------|-------|-----------|
| 1 | action_grab_left | 前(dir=1) | 190 | 9000 |
| 2 | action_gouto_xiangzi_first | 后(dir=0) | 102 | 5000 |
| 3 | action_gouto_xiangzi_first | 后(dir=0) | 158 | 8000 |
| 4 | action_gouto_xiangzi_first | 后(dir=0) | 52 | 3000 |
| 5 | action_gouto_xiangzi_first | 后(dir=0) | 8.5 | 1500 |
| 6 | action_grab_middle | 前(dir=1) | 120 | 6000 |
| 7 | action_grab_middle | 前(dir=1) | 195 | 8000 |
| 8 | action_gouto_xiangzi_secend | 后(dir=0) | 100 | 6000 |
| 9 | action_gouto_xiangzi_secend | 后(dir=0) | 215 | 11500 |
| 10 | action_grab_right | 前(dir=1) | 120 | 6000 |
| 11 | action_grab_right | 前(dir=1) | 195 | 9000 |
| 12 | action_gouto_xiangzi_secend | 后(dir=0) | 100 | 6000 |
| 13 | action_gouto_xiangzi_secend | 后(dir=0) | 215 | 11500 |

---

## 六、历史修改记录

### 6.1 单爪结构重构 (2026-08-01)

从双爪结构改为单爪结构(仅黑爪), 抓豆分3次(左->中->右), 每次抓完去箱子放豆。

**删除的函数**:
- `action_test()` - 旧的单次抓放流程
- `action_douzi_first()` - 旧的双爪第一次抓(左+中)
- `action_douzi_secend()` - 旧的双爪第二次抓(右)
- `place_beans()` - 旧的分拣决策(文字模拟)

**新增的函数**:
- `action_grab_left()` - 第1次抓豆(左边, X=+35): 组15准备->Z降30->组16抓->Z升30->组12过渡->X-72走到右豆方向
- `action_grab_middle()` - 第2次抓豆(中间, X=+14.5): 从箱回豆区(Y+120)->X-82 Y+195到右豆->X+24到中豆->组3准备->Z降20->组4抓->Z升20->组12过渡->X-24回右豆
- `action_grab_right()` - 第3次抓豆(右边, X=-57.5): 从箱回豆区(Y+120)->X-82 Y+195到右豆->组15准备->Z降25->组16抓->Z升25->组12过渡

**修改的函数**:
- `action_all()` - 重写为3次抓放流程, 仅第1次抓豆前Phase1, 仅第1次放箱Phase2/Phase3
- `action_gouto_xiangzi_secend()` - 移除runActionGroup(12,1), 组12已移至各抓豆函数末尾
- `goto_box()` - 移除claw参数, 仅黑爪: 准备组(7/9/12)+组18开爪
- `action.h` - 移除CLAW_WHITE/CLAW_BLACK定义, goto_box签名改为单参数

**舵机动作组变更**:

| 旧动作组 | 新动作组 | 用途 |
|---------|---------|------|
| 组1(舵机初始化) | 组15 | 黑爪准备抓左/右边豆子 |
| 组2(白爪抓) | 组16 | 黑爪抓左/右边豆子 |
| 组3(转轴抓中间) | 组3(不变) | 黑爪准备抓中间豆子 |
| 组4(黑爪抓) | 组4(不变) | 黑爪抓中间豆子 |
| 组5(转轴放箱准备) | 组12 | 抓豆后过渡 |
| 组12(放箱准备) | 组12(不变) | goto_box中放箱准备 + 抓豆后过渡 |

**豆子X轴坐标** (以初始点为原点, 靠墙dir=1为正):
- 左豆: +35 (dir=1 靠墙走35)
- 中豆: +14.5 (从左豆dir=0走20.5)
- 右豆: -57.5 (从中豆dir=0走72)
- 4号箱: ≈+24.5 (由累计移动推算)
- 从4号箱到中豆: dir=0走10cm
- 从4号箱到右豆: dir=0走82cm
- 从右豆到中豆: dir=1走24cm
- 从中豆到右豆: dir=0走24cm

### 6.2 K230 协议层移植 (从 action.c -> K230.c)

将 action.c 中的 K230 通讯协议层(5个static函数 + 3个公开函数)移植到 K230.c:
- 内部函数加 `k230_` 前缀避免命名冲突
- 三个超时/重试宏移至 K230.h
- action.c 通过 `k230_phase1/2/3` 调用, 不再直接操作通讯细节变量

### 6.3 函数重命名

| 原名 | 新名 |
|------|------|
| `action_1` | `action_test` |
| `action_xiangzi_first` | `action_gouto_xiangzi_first` |
| `Data_Handle1` | `Data_yinshe` |
| `goto_box` (static) | `goto_box` (公开, action.h 声明) |

### 6.4 编译错误修复

| 错误 | 修复 |
|------|------|
| `action_gouto_xiangzi` implicit declaration | 改为 `action_gouto_xiangzi_first()` |
| `s_current_box` undefined | 定义移至 `action_all` 之前 |
| `action_douzi_secend` incompatible declaration | 前向声明区新增 |
| `bean_color[3]` subscript out of range | 改为 `bean_color[2]` |
| `action_all` 未前向声明 | 新增前向声明 |
| 文件末尾无换行 | 末尾加空行 |
| `dbg` 链接错误(app_init.c) | 替换为 `printf` |
| `goto_box` 链接错误(app_init.c) | 去掉 static, 在 action.h 公开声明 |

### 6.5 Keil 工程

- `cantest.uvprojx` 新增 `Docs` 组, 包含 `蓝安的代码内容.md`(FileType=5 文档类型)

---

## 七、注意事项

1. **MicroLIB 必须勾选**: CubeMX 重生会丢, 丢失则 printf 卡死无输出。见 [[keil-microlib-required]]
2. **defaultTask 栈大小**: 512B 跑 App_Init 会溢出静默 HardFault。见 [[default-task-stack-overflow]]
3. **Y轴双电机镜像**: Y2 用 `!y_dir` 取反, 因安装方向镜像。见 [[y-motor-mirror-install]]
4. **Emm_V5.h 与 Emm_V5_CAN.h 不可同文件include**: S_VER 等枚举冲突
5. **日志一律用英文**: UTF-8 中文字符串在 ARMCC V5.06 下会乱码/报错
6. **代码改动更新md**: 每次改代码后必须同步更新本文档。见 [[update-md-on-code-change]]

---

## 八、合并 upstream/main K230 协议层根治

### 8.1 合并策略

| 文件 | 来源 |
|------|------|
| K230.c / K230.h | upstream 协议根治版本, 函数名保持 `Data_yinshe` |
| action.c | 保留本地版本 |
| app_init.c | 保留本地版本 |
| action.h | 保留本地版本 |
| uart_callback.c, lift.c, motor.h | upstream 版本 |
| view/*.py, 文档 | upstream 版本 |

### 8.2 K230 协议层新增内容 (来自 upstream)

| 新增项 | 说明 |
|--------|------|
| `k230_wait_ready()` | 上电握手: 阻塞等 K230 就绪帧(0x0B), 模型加载完成才发命令 |
| `K230_CMD_RESYNC` (0x0C) | 重同步命令: 数据超时时先发 RESYNC 让 K230 重发上一次回复, 不再发 CLOSE(会让 K230 误判阶段完成而前进) |
| 阶段号 byte[1] | 发出帧的 byte[1] 改为承载阶段号(1=bean, 2=front, 3=side), 两端据此丢弃跨阶段迟到帧 |
| `k230_ready_flag` | K230 就绪标志(0x0B 帧触发) |
| `k230_last_rx_tick` | 最近收到合法帧的 tick, 静默超 3s 示警, 区分"K230 挂了"和"识别慢" |
| `k230_rx_badphase` | 阶段号不符被丢弃的帧数(诊断用) |
| `K230_DATA_TIMEOUT_MS` | 15s -> 25s, 必须大于 K230 端 20s 识别超时, 否则出现错位窗口 |
| `K230_READY_TIMEOUT_MS` | 30s, 等模型加载 |
| `K230_RESYNC_TIMEOUT_MS` | 3s, 等一帧重传 |
| `K230_SILENT_WARN_MS` | 3s, 静默示警阈值 |

### 8.3 命名保留

upstream 把 `Data_yinshe` 改回了 `Data_Handle1`, 合并时保持本地命名 `Data_yinshe`, sed 全局替换回 `Data_yinshe`。

### 8.4 goto_box 单爪简化

原 `goto_box(target_box, claw)` 有 claw 参数支持白爪/黑爪, 单爪重构后移除 claw 参数, 仅保留黑爪逻辑:

```c
void goto_box(uint8_t target_box);
```

准备动作组(仅黑爪):

| 箱子1 | 箱子2-4(中间) | 箱子5 |
|-------|--------------|-------|
| 组7 | 组12 | 组9 |

开爪放豆子: 组18
