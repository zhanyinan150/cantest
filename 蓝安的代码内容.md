# 蓝安的代码内容

## 修改概述

1. 将 `modules/action/action.c` 中的 **K230 通讯协议层** 移植到 `modules/k230/K230.c` 和 `modules/k230/K230.h`，实现通讯协议与动作序列的解耦。`action.c` 通过 `#include "K230.h"` 直接调用协议层接口。
2. 恢复 `action_douzi_first()` 的真实动作：`runActionGroup` 全部启用，Z 轴升降全部恢复。

---

## 修改文件清单

| 文件 | 操作 |
|------|------|
| `modules/k230/K230.h` | 新增协议层宏定义 + 三个阶段函数声明 |
| `modules/k230/K230.c` | 新增头文件引用 + 协议层函数实现（8个函数） |
| `modules/action/action.c` | 移除协议层代码；恢复 `action_douzi_first()` 真实动作（runActionGroup + Z轴升降） |

---

## 一、K230.h 修改内容

### 新增：K230 通讯协议层声明（`#endif` 之前插入）

```c
/* ==================== K230 通讯协议层 (三阶段主机通讯) ==================== */
/* 从 action.c 移植而来: 发命令->等ACK->等数据->回ACK 的三阶段封装,
 * 与 k230_competition.py 的 Phase1/2/3 一一对应, 由 action.c 调用。 */

/* ---- K230 通讯超时/重试 ---- */
#define K230_ACK_TIMEOUT_MS      3000    /* 等对端 ACK */
#define K230_DATA_TIMEOUT_MS     15000   /* 等识别结果(含推理耗时) */
#define K230_RETRY_MAX           3       /* 重试次数, 只在一层生效, 勿嵌套 */

int k230_phase1_bean(void);    /* Phase1: 豆子颜色识别 */
int k230_phase2_front(void);   /* Phase2: 正面数字识别 */
int k230_phase3_side(void);    /* Phase3: 侧面数字 + 推理第5位 */
```

**说明**：三个宏（`K230_ACK_TIMEOUT_MS` / `K230_DATA_TIMEOUT_MS` / `K230_RETRY_MAX`）从 action.c 移至 K230.h，供 K230.c 内部协议层函数使用。

---

## 二、K230.c 修改内容

### 2.1 新增头文件引用

```c
#include "cmsis_os2.h"        /* osDelay (协议层重试延时) */
#include "FreeRTOS.h"
#include "task.h"
#include "bsp_log.h"          /* printf -> 日志队列 -> LogTask DMA 发 USART1 */
#include <stdio.h>            /* snprintf (协议层格式化输出) */
```

**原因**：协议层函数用到 `osDelay`（FreeRTOS）、`printf`（bsp_log 队列）、`snprintf`（格式化日志），需引入对应头文件。

### 2.2 新增文件头说明

在 K230.c 文件头注释中追加：

```
 * K230 三阶段通讯协议层(从 action.c 移植): 发命令->等ACK->等数据->回ACK
 *   的三阶段封装, 带 ACK/数据超时重试(最多 K230_RETRY_MAX 次)。
 *   k230_phase1_bean / k230_phase2_front / k230_phase3_side 供 action.c 调用。
```

### 2.3 新增协议层函数（追加在 `Data_yinshe` 之后）

#### 内部静态函数（5个，`static`，加 `k230_` 前缀避免命名冲突）

| 原名（action.c） | 新名（K230.c） | 功能 |
|---|---|---|
| `dbg()` | `k230_dbg()` | 调试输出，走 printf -> 日志队列 -> LogTask DMA 发 USART1 |
| `wait_flag()` | `k230_wait_flag()` | 等待事件标志（计数器语义），成功后消费一个事件 |
| `send_cmd_once()` | `k230_send_cmd_once()` | 发一次命令 + 等一次 ACK，不重试 |
| `send_cmd_and_wait_ack()` | `k230_send_cmd_and_wait_ack()` | 发命令 + 等 ACK，超时重试最多 K230_RETRY_MAX 次 |
| `send_cmd_wait_ack_wait_data()` | `k230_send_cmd_wait_ack_wait_data()` | 发命令 + 等 ACK + 等数据，任一步超时则重发 |

#### 公开函数（3个，`extern`，供 action.c 调用）

| 函数名 | 功能 | 返回值 |
|---|---|---|
| `k230_phase1_bean()` | Phase1: 豆子颜色识别，结果填入 `bean_color[3]` | 0=成功, -1=重试耗尽 |
| `k230_phase2_front()` | Phase2: 正面数字识别（K230端自存，不发数据帧，只发两个ACK） | 0=成功, -1=重试耗尽 |
| `k230_phase3_side()` | Phase3: 侧面数字 + 推理第5位，结果填入 `number_position[5]` | 0=成功, -1=重试耗尽 |

**重命名原因**：内部静态函数加 `k230_` 前缀，与 K230.c 已有的 `k230_arm_rx`、`k230_write`、`k230_read` 等命名风格一致。公开的三个阶段函数保持原名 `k230_phase1/2/3`，action.c 调用处无需改名。

---

## 三、action.c 修改内容

### 3.1 文件头注释更新

- 标题从「动作序列 + K230 三阶段通讯(主机)」改为「动作序列 + K230 三阶段通讯**调用**(主机)」
- 删除 K230 协议细节描述（已移至 K230.h）
- 新增说明：协议层已移至 `modules/k230/K230.c`，本文件通过 `k230_phase1/2/3` 调用

### 3.2 移除的内容

| 移除项 | 原行号 | 说明 |
|---|---|---|
| `K230_ACK_TIMEOUT_MS` 宏 | 48 | 移至 K230.h |
| `K230_DATA_TIMEOUT_MS` 宏 | 49 | 移至 K230.h |
| `K230_RETRY_MAX` 宏 | 50 | 移至 K230.h |
| `dbg()` 函数 | 83-86 | K230.c 中新建 `k230_dbg()` 代替 |
| `wait_flag()` 函数 | 93-105 | 移至 K230.c，改名 `k230_wait_flag()` |
| `send_cmd_once()` 函数 | 113-123 | 移至 K230.c，改名 `k230_send_cmd_once()` |
| `send_cmd_and_wait_ack()` 函数 | 126-135 | 移至 K230.c，改名 `k230_send_cmd_and_wait_ack()` |
| `send_cmd_wait_ack_wait_data()` 函数 | 140-163 | 移至 K230.c，改名 `k230_send_cmd_wait_ack_wait_data()` |
| `k230_phase1_bean()` 函数 | 171-187 | 移至 K230.c（公开函数） |
| `k230_phase2_front()` 函数 | 193-224 | 移至 K230.c（公开函数） |
| `k230_phase3_side()` 函数 | 230-247 | 移至 K230.c（公开函数） |

### 3.3 保留的内容

| 保留项 | 说明 |
|---|---|
| `dbg()` 函数 | action.c 自身调试输出仍需要（place_beans / 动作序列日志） |
| `place_beans()` 函数 | 分拣决策逻辑，属于动作层而非协议层 |
| `action_test()` 任务 | 主流程编排 |
| `action_douzi_first()` | 抓豆子动作序列 |
| `action_gouto_xiangzi()` | 放箱子动作序列（内含 Phase2/Phase3 调用） |
| `#include "K230.h"` | 已有，无需新增--协议层函数声明已在 K230.h 中 |

### 3.4 调用关系（未改变）

```
action_test()
  ├── k230_phase1_bean()          ← 现在从 K230.c 调入
  ├── action_douzi_first()
  ├── action_gouto_xiangzi()
  │     ├── k230_phase2_front()   ← 现在从 K230.c 调入
  │     └── k230_phase3_side()    ← 现在从 K230.c 调入
  └── place_beans()
        └── Data_yinshe()        ← 本就在 K230.c
```

---

## 四、命名对照表

| action.c 原名 | K230.c 新名 | 可见性 | 说明 |
|---|---|---|---|
| `dbg` | `k230_dbg` | static | 两处各有一个 static `dbg`/`k230_dbg`，互不冲突 |
| `wait_flag` | `k230_wait_flag` | static | 加前缀，与 K230.c 命名风格一致 |
| `send_cmd_once` | `k230_send_cmd_once` | static | 同上 |
| `send_cmd_and_wait_ack` | `k230_send_cmd_and_wait_ack` | static | 同上 |
| `send_cmd_wait_ack_wait_data` | `k230_send_cmd_wait_ack_data` | static | 同上 |
| `k230_phase1_bean` | `k230_phase1_bean` | extern | 名称不变，从 static 改为 extern |
| `k230_phase2_front` | `k230_phase2_front` | extern | 同上 |
| `k230_phase3_side` | `k230_phase3_side` | extern | 同上 |

---

## 五、依赖关系变化

### 修改前

```
action.c  ──includes──>  K230.h  (k230_write, bean_color, Data_yinshe ...)
action.c  ──内部定义──>  wait_flag, send_cmd_*, k230_phase1/2/3  (static)
```

### 修改后

```
action.c  ──includes──>  K230.h  (k230_write, bean_color, Data_yinshe,
                                  k230_phase1/2/3, K230_ACK/DATA/RETRY 宏)
                                    │
K230.c    ──implements──> k230_phase1/2/3 (extern)
                          k230_dbg, k230_wait_flag, k230_send_cmd_* (static)
```

**变化要点**：action.c 不再直接操作 `k230_ack_flag`、`bean_flag`、`full_number_flag` 等通讯细节变量，只通过 `k230_phase1/2/3` 三个高层接口与 K230 交互。协议层的超时/重试逻辑完全封装在 K230.c 内部。

---

## 六、action_douzi_first() 恢复真实动作

### 6.1 改动概述

`action_douzi_first()`（抓豆子动作序列）此前的联调阶段配置：
- Z 轴升降全部置 0（仅第 1 步保留上升 35cm）
- `runActionGroup` 全部注释，改 `dbg("[SERVO-SIM]...")` 文字占位

现已恢复为真实动作：5 个 `runActionGroup` 全部启用，Z 轴升降全部恢复原始参数。同时修正了原来从第 1 步复制过来的错误注释（X/Y distance=0 时注释却写 44cm/185cm，Z dir=1 时注释却写 dir=0(上)）。

### 6.2 改动对照表

| 步骤 | 联调阶段（旧） | 真实动作（新） |
|---|---|---|
| 1/7 起始->左豆 | Z+35, `runActionGroup` 注释 | Z+35, `runActionGroup(1,1)` 舵机初始化 |
| 2/7 降爪抓左豆 | Z=0 不动, `dbg("[SERVO-SIM]")` | Z-30 下降, `runActionGroup(2,1)` 白爪抓 |
| 3/7 升爪复位 | Z=0 不动, `dbg("[SERVO-SIM]")` | Z+30 上升 |
| 4/7 ->中间豆 | X-20.5, `runActionGroup` 注释 | X-20.5, `runActionGroup(3,1)` 转轴 |
| 5/7 降爪抓中豆 | Z=0 不动, `dbg("[SERVO-SIM]")` | Z-20 下降, `runActionGroup(4,1)` 黑爪抓 |
| 6/7 升爪复位 | Z=0 不动, `dbg("[SERVO-SIM]")` | Z+20 上升 |
| 7/7 ->放箱准备 | X-60, `runActionGroup` 注释 | X-60, `runActionGroup(5,1)` 转轴准备 |

### 6.3 删除的内容

- 所有 `/* runActionGroup(N, 1); */` 注释行 -> 替换为真实 `runActionGroup(N, 1);` 调用
- 所有 `dbg("[SERVO-SIM] >>> ... <<<\r\n");` 占位行
- `@note` 中关于联调阶段 Z/SKIPPED/servo text only 的说明
- dbg 日志中 `[Z SKIPPED, orig: ...]` 标记，改为真实动作描述如 `(Z-30)` / `(Z+30)`

### 6.4 保留未改的部分

- `place_beans()` 仍为文字模拟（`Action_1..5` 仍是 K230.c 里的 `__weak` 空桩）
- `action_gouto_xiangzi()`（放箱子序列）未改动
- 文件头注释中的联调阶段说明（第 18-22 行）尚未同步更新，仍提到"Z 只执行第1步/舵机注释占位"，与 `action_douzi_first()` 实际代码不符

---

## 七、箱子间X轴移动函数

### 7.1 需求

`action_gouto_xiangzi()` 运动到第4个箱子后，Y/Z 不动，只动 X 轴前往其他箱子（也可能在第4个箱子原地不动），然后再前往第 i 个箱子。只做移动，不做放料动作。

### 7.2 箱子间距数据（以第4个箱子为原点）

| 目标箱子 | X 坐标(cm) | 方向 | dir 值 |
|----------|-----------|------|--------|
| 第1个箱子 | -93 | 左(远离墙) | 0 |
| 第2个箱子 | -73 | 左(远离墙) | 0 |
| 第3个箱子 | -35 | 左(远离墙) | 0 |
| 第4个箱子 | 0 | 不动 | - |
| 第5个箱子 | +25 | 右(靠近墙) | 1 |

- 箱子编号 1~5 从左到右，与 K230 `number_position` 一致
- 以第4个箱子为坐标原点，左为负、右为正
- 方向定义：`dir=0`(左/远离墙)，`dir=1`(右/靠近墙)

### 7.3 新增函数

#### `goto_box(uint8_t target_box)` - 箱子间X轴直线移动

- **逻辑**：以第4个箱子为坐标原点，各箱子有固定坐标值，直接用坐标差算位移：
  - `delta = 目标箱子坐标 - 当前箱子坐标`
  - `delta > 0`：向右(dir=1)，距离 = delta
  - `delta < 0`：向左(dir=0)，距离 = -delta
  - 一步直达，不经第4个箱子中转
- **Y/Z 不动**：Motor_XYZ 的 Y/Z 参数全填 0
- **只移动不放料**：到达后直接返回，不执行舵机动作
- **位置跟踪**：`s_current_box` 静态变量记录当前所在箱子，初始值 4（`action_gouto_xiangzi` 结束后位于第4个箱子）

### 7.4 数据结构

```c
/* 以第4个箱子为原点(0), 左为负右为正, index 1~5 对应箱子1~5 */
static const float box_x_coord[6] = {0, -93.0f, -73.0f, -35.0f, 0.0f, 25.0f};
static uint8_t s_current_box = 4;  /* 当前所在箱子, action_gouto_xiangzi 结束后为4 */
```

### 7.5 调用示例

```c
/* action_gouto_xiangzi() 结束后, 机器人位于第4个箱子(坐标0) */
goto_box(2);   /* 0-(-73)=73cm 向左, 去第2个箱子 */
goto_box(5);   /* -73-25=-98 -> 98cm 向右, 去第5个箱子 */
goto_box(4);   /* delta=0, 不移动 */
```

---

## 八、goto_box 公开化 + app_init.c 调用

### 8.1 goto_box 从 static 改为公开函数

**改动原因**：`app_init.c` 中的 `MotorAutoTask` 需要调用 `goto_box`，但 `goto_box` 原本是 `static` 函数，只在 action.c 内部可见，链接器报 `Undefined symbol goto_box`。

**改动内容**：

| 文件 | 改动 |
|------|------|
| `modules/action/action.c` | `goto_box` 声明和定义去掉 `static` 关键字（2处） |
| `modules/action/action.h` | 新增 `void goto_box(uint8_t target_box);` 声明 |
| `modules/action/action.h` | 新增 `#include <stdint.h>`（声明中使用了 `uint8_t`） |

### 8.2 app_init.c 调用 goto_box

`MotorAutoTask` 中调用 `goto_box` 进行箱子间移动：

```c
goto_box(2);   /* 去第2个箱子 */
goto_box(5);   /* 去第5个箱子 */
goto_box(3);   /* 去第3个箱子 */
```

### 8.3 drop_bean_at_box 移除

`action_think_2` 分支中移除了 `drop_bean_at_box` 函数。箱子间移动逻辑由 `goto_box` 单独承担，放料动作在调用方自行处理 `runActionGroup`。

---

## 九、函数重命名 + 动作序列重构

### 9.1 函数重命名

| 原名 | 新名 | 文件 | 说明 |
|------|------|------|------|
| `action_1` | `action_test` | action.c | 任务函数重命名 |
| `action_xiangzi_first` | `action_gouto_xiangzi_first` | action.c | 放箱子函数重命名 |
| `Data_Handle1` | `Data_yinshe` | K230.c/h | 豆子颜色映射函数重命名 |
| `goto_box` (static) | `goto_box` (公开) | action.c/h | 去掉 static, 在 action.h 声明 |

### 9.2 动作序列重构

- 新增 `action_all`: 完整比赛流程(两次抓放)
- 新增 `action_gouto_xiangzi_secend`: 第二次放箱子
- 动作拆分为两次: 先抓左边+中间豆子放箱, 再抓右边豆子放箱
- `place_beans` 后调用 `goto_box(Data_yinshe(bean_color[x]))` 实际走到目标箱子放豆子

### 9.3 Data_yinshe (原 Data_Handle1) 改动

- 注释掉内部 switch 语句(不再调 Action_1..5 空桩)
- 只返回箱子位置 `pos`(1~5), 由调用方决定放料动作

### 9.4 app_init.c 改动

- MotorAutoTask 动作参数调整(X 65cm, Z 35cm)
- `goto_box` 调用暂时注释

### 9.5 Keil 工程

- `cantest.uvprojx` 新增 `Docs` 组, 包含 `蓝安的代码内容.md`(FileType=5 文档类型)
