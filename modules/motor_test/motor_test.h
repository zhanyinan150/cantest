/**
  ******************************************************************************
  * @file    motor_test.h
  * @brief   CAN2 步进电机独立测试 (Modules层)
  ******************************************************************************
  * 用途: 向 ID=1 的 Emm_V5 步进电机发送位置模式 CAN 命令,
  *       前后反转运动, 每次移动 20cm, 到位后反向。
  *       USART1 打印每帧 CAN (帧日志), 供逻辑分析仪对照。
  ******************************************************************************
  */
#ifndef __MOTOR_TEST_H
#define __MOTOR_TEST_H

#include "main.h"
#include <stdint.h>
#include <stdbool.h>

/* 测试目标电机地址 (CAN2 扩展帧) */
#define MOTOR_TEST_ADDR     1

/**
  * @brief  CAN2 步进电机独立测试入口 (自包含)
  * @note   内部完成: CAN2 启动 -> 注册 ID=1 步进 -> 使能 -> 打开帧日志 -> 创建测试任务。
  *         测试任务循环发送位置模式命令 (交替方向, 来回运动)。
  */
void MotorTest_Init(void);

#endif /* __MOTOR_TEST_H */
