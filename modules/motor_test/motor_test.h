/**
  ******************************************************************************
  * @file    motor_test.h
  * @brief   CAN2 步进电机独立测试 (Modules层)
  ******************************************************************************
  * 用途: 向 ID=1,2 的两只 Emm_V5 步进电机发送位置模式 CAN 命令,
  *       多机同步前后反转运动, 每次移动 50cm, 到位后反向。
  *       两电机参数完全相同, snF=true 预存 + 广播同步启动。
  *       USART1 打印每帧 CAN (帧日志), 供逻辑分析仪对照。
  ******************************************************************************
  */
#ifndef __MOTOR_TEST_H
#define __MOTOR_TEST_H

#include "main.h"
#include <stdint.h>
#include <stdbool.h>

/* 测试目标电机地址 (CAN2 扩展帧), 两电机参数相同仅 ID 不同 */
#define MOTOR_TEST_ADDR_1     1
#define MOTOR_TEST_ADDR_2     2

/**
  * @brief  CAN2 步进电机独立测试入口 (自包含)
  * @note   内部完成: CAN2 启动 -> 注册 ID=1,2 步进 -> 同步使能 -> 打开帧日志 -> 创建测试任务。
  *         测试任务循环发送位置模式命令 (两电机同步, 交替方向, 来回运动)。
  */
void MotorTest_Init(void);

#endif /* __MOTOR_TEST_H */
