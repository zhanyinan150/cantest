# 代码审核与修复记录

审核日期：2026-07-27
审核范围：`app/` `bsp/` `modules/` `Core/Src`(用户部分) `view/*.py` + CubeMX 配置
未审范围：`Drivers/`(ST HAL) `Middlewares/`(FreeRTOS / USB) — 原厂代码

构建结果：**0 Error / 0 Warning**（ARMCC V5.06 update 5，Code=36384 RO=668 RW=560 ZI=44152）

---

## 一、本次修复清单

### P0-1 五个串口的 NVIC 全局中断全部未使能 ★根因级

**现象**：`cantest.ioc` 无任何 `NVIC.USARTx_IRQn` 条目，`stm32f4xx_it.c` 无 `USARTx_IRQHandler`，
`usart.c` 的 MspInit 只配了 GPIO + DMA。只有 DMA 流中断和 CAN 中断被使能。

**为什么致命**（对照 HAL 源码 `stm32f4xx_hal_uart.c`）：

```c
UART_DMATransmitCplt()      // DMA 传输完成中断里只做这一件事：
    ATOMIC_SET_BIT(CR1, USART_CR1_TCIE);   // 置 USART 的 TC 中断使能位后返回
    // ← 不调用 HAL_UART_TxCpltCallback，也不复位 gState

UART_EndTransmit_IT()       // gState 复位 + TxCpltCallback 只发生在这里
    huart->gState = HAL_UART_STATE_READY;
    HAL_UART_TxCpltCallback(huart);
    // ← 只能由 HAL_UART_IRQHandler 在 USART TC 中断中调用
```

USART1_IRQn 未使能 → TC 中断永不进入 → `huart1.gState` 永久停在 `HAL_UART_STATE_BUSY_TX`。

| 受影响功能 | 位置 | 表现 |
|---|---|---|
| 日志系统 | `bsp_log.c` | 第 1 行 DMA 发出后信号量永不释放，之后所有 `Transmit_DMA` 返回 HAL_BUSY，**printf/LOG 只出第一行就死** |
| action 调试输出 | `action.c` | `HAL_UART_Transmit` 入口检查 `gState == READY`，日志跑过一次后**全部返回 HAL_BUSY** |
| VOFA 命令接收 | `uart_callback.c` | `HAL_UART_Receive_IT` 依赖 RXNE 中断，**一条命令都收不到** |
| 遥测波形 | `bsp_vofa.c` | 忙等 gState 20ms 后 Abort 丢帧，每帧白烧 20ms CPU |
| X 轴步进 | `Emm_V5.c` | 作者已踩到，注释直言「DMA 完成中断未触发」，用 `AbortTransmit` 硬复位绕过 |

> 这正是 memory 里记的坑「HAL_UART_Receive 阻塞 LogTask 致 printf 无输出」的**真实根因**。
> 之前之所以看似能跑，是因为电机模块全被 `#if 0`，没人往日志队列塞东西，
> LogTask 一直阻塞在 `osMessageQueueGet`，gState 侥幸保持 READY。
> 一旦恢复 `Lift_Init()`/`Motor_Init()`（里面全是 printf），USART1 会立刻锁死。

**修复**：
- `Core/Src/usart.c` — USART1 / USART3 / UART5 的 MspInit 各补 `HAL_NVIC_SetPriority(x, 5, 0)` + `HAL_NVIC_EnableIRQ(x)`
- `Core/Src/stm32f4xx_it.c` — 新增 `USART1_IRQHandler` / `USART3_IRQHandler` / `UART5_IRQHandler`
- `cantest.ioc` — 补 3 条 NVIC 记录，避免 CubeMX 重新生成时再次丢失

优先级 5 与 DMA/CAN 一致，等于 `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY`，可安全调用 FromISR API。

> USART2 / UART4 未启用中断：USART2 全工程无人使用；UART4(舵机) 用阻塞
> `HAL_UART_Transmit`，不依赖中断。需要时按同样方式补。

---

### P0-2 K230 串口一旦错帧永久失效 + 无错误恢复

**问题 A — 帧同步无法自愈**：原用 `HAL_UART_Receive_DMA(&huart3, buf, 8)` 定长收帧。
一旦丢/多一个字节，DMA 组帧相位永久错位，XOR 永远失败且无法重新对齐。

**问题 B — 无错误恢复**：全工程没有 `HAL_UART_ErrorCallback`。USART3 一次 ORE/FE
（上电噪声很常见）就会让 HAL 中止接收并调用空的弱回调，**K230 通讯静默永久断开**。
且 `HAL_UART_Receive_DMA` 的返回值全部未检查。

**修复**：
- `bsp/uart/uart_callback.{c,h}` — 扩展分发表，新增两类回调注册：
  - `UART_Callback_RegisterEvent()` → `HAL_UARTEx_RxEventCallback`（IDLE 切帧，带实际长度）
  - `UART_Callback_RegisterRestart()` → 错误恢复
  - 新增全局唯一的 `HAL_UART_ErrorCallback`：清 ORE/NE/FE/PE 后重新武装接收
    （USART1 内建恢复，其它串口调注册的 restart 回调）
- `modules/k230/K230.c` — 改用 `HAL_UARTEx_ReceiveToIdle_DMA`，靠帧间空隙自动重新对齐；
  长度 ≠ 8 视为残帧丢弃；关掉 DMA 半满中断避免 4 字节就上报；所有武装调用检查返回值
- 新增 4 个诊断计数器（调试器可观察是否在丢帧）：
  `k230_rx_frames` / `k230_rx_badxor` / `k230_rx_badlen` / `k230_rx_rearm_err`

---

### P0-3 第 5 个数字推理失败会让整场比赛卡死

**问题**：`k230_competition.py::infer_fifth()` 在 4 个已知数字不互异时返回 `0x00`。
而 STM32 端 `K230.c` 解析 0x01 帧要求 `byte[2..6]` **全部非 0**：

```c
if (K230_Rx[2] != 0 && ... && K230_Rx[6] != 0) { ...; full_number_flag = 1; }
```

→ `full_number_flag` 永不置位 → 主机重试 3 次 ×(3s+15s) 全超时 → `goto idle`
→ **三颗豆子一颗都放不出去**。侧面数字与正面某个数字识别成同一个是很容易发生的。

**修复**（`view/k230_competition.py`）：
- `infer_fifth` 保证总是返回合法数字(1~5)：4 个互异时逻辑不变；有重复时返回任一未占用的
  数字并打印告警；5 个全占满退回 `0x01`
- 发送前加最后一道兜底：`full_result` 里若仍有 `0x00`，用未占用的数字补齐
- 侧面识别接收 `exclude=front_numbers`，兜底时挑不与正面重复的数字

设计取舍：**放错箱子好过整场卡死**，降级路径都会打 `[WARN]`，赛后可从日志判断是否走过。

---

### P0-4 K230 端 UART 读取会永久丢帧

**问题**：`data = uart.read()` 无参数读走全部可用字节，然后 `if len(data) >= 8` 只看前 8 个。
- 读到 3 字节半帧 → `len < 8` → **整包丢弃，那几个字节再也拿不回来**，后续永久错位
- 读到 16 字节两帧（STM32 重试时必然发生）→ **第二帧静默丢掉**

且与 `K230比赛通讯协议.md` 第 22 行写的「`uart.any() >= 8` 后 `uart.read(8)`」不符。

**修复**：新增 `poll_frames()` —— 累积缓冲 `_rx_acc` + 逐帧 XOR 校验 + 校验失败滑窗 1 字节重试，
掉字节后自动重新对齐且不丢帧。新增 `flush_rx()`，阶段切换时清掉上一阶段的重发残帧。
超时判断改用 `time.ticks_diff()` 处理计数回绕。

---

### P0-5 K230 识别循环无超时，导致双向死锁

**问题**：三个识别函数都是 `while True`，只在识别到稳定结果时才返回。视野遮挡/光照不足/
模型漏检时永远出不来，且循环里完全不读 UART。STM32 15s 超时后重发命令 K230 根本收不到，
**死锁到比赛结束**。

**修复**：三个循环统一加 `RECOGNIZE_TIMEOUT_MS = 20000` 超时 + 尽力而为兜底：
优先返回最近一次完整但未稳定的结果；一次都没识别到时返回合法占位值
（豆子 `g/w/y`，正面数字 `1/2/3`，侧面数字挑一个不与正面重复的），全部打 `[WARN]`。

---

### P1-6 `Emm_V5_CAN_Read_Flag/Read_Encoder` 会把 M2006 误认成 Y2 步进

**问题**：查找实例时只比地址、不检查 `use_ext_id`：

```c
uint8_t can_addr = (can_instance[i]->rx_id >> 8) & 0xFF;
if (can_addr == addr) { target = can_instance[i]; break; }
```

M2006 反馈 ID = `0x201`，`(0x201 >> 8) & 0xFF = 0x02` —— **正好等于 Y2 步进的地址 2**。
`app_init.c` 里 `Lift_Init()` 排在 `Emm_V5_CAN_Init()` 之前，M2006 会占 `can_instance[0]`，
于是 `Read_Flag(2)` 第一轮就命中它，**Y2 的到位/堵转检测全部读错对象**。

**修复**：抽出 `Emm_V5_CAN_FindByAddr()`，跳过标准帧实例（大疆电机），两处调用点统一使用。
（`EmmV5_CAN_SendCmd` 的查找原本就有 `use_ext_id` 保护，只有这两个读取函数漏了。）

---

### P1-7 `action.c` 的重试会打乱协议状态机 + flag 时序竞态

**问题**：
1. 数据超时后直接重发识别命令，但此时 K230 卡在 `wait_for_cmd(0x06)` 等 ACK，只认 0x06，
   重发的 0x02/0x03/0x01 会被丢弃，双方从此失步，剩余重试全部空转
2. `data_flag` 在发命令前从未清零。STM32 复位而 K230 未复位时，残留的 `bean_flag=1` 会让
   `wait_flag` 立即返回成功，但 `bean_color` 是上一轮的旧数据
3. Phase 2 用同一个 `0x0A` 区分「命令收到」和「识别完成」，而 `k230_ack_flag` 是布尔量。
   K230 识别很快时两个 ACK 间隔极短，`k230_ack_flag = 0` 会把第二个抹掉 → 白等 15s
4. `dbg()` 直接 `HAL_UART_Transmit(&huart1,...)`，与 LogTask 的 DMA 抢同一个 USART1 ——
   正是 `bsp_log.c` 文件头列为 HardFault 根因的用法

**修复**：
- `k230_ack_flag` 改为**计数器**（`K230.c` 收到 0x0A 时 `++` 而非置 1），`wait_flag` 成功后
  `(*flag)--` 消费一个事件而非粗暴清零；Phase 2 等第二个 ACK 前不再清零
- 重试前先补发 `CLOSE(0x06)` 让对端退回空闲态，`osDelay(200)` 后再重发命令
- 发命令前显式清 `data_flag`
- `k230_write` 改为返回 `int`，发送失败时重试
- `dbg()` 改走 `printf` → 日志队列 → LogTask，USART1 恢复单一所有者

---

### P1-8 `Data_Handle1` 静默丢弃豆子

**问题**：`number_position` 里找不到目标数字时 `pos` 保持 0，switch 走 default 什么都不做，
**豆子被无声丢弃，现场完全看不出发生了什么**。

**修复**：改为返回 `int`（>0 命中位置 / -1 颜色非法 / -2 数字不在表中）；
`action.c` 改用循环调用并打印每颗豆子的去向或 `SKIPPED (err N)`。

---

### P2 其它修复

| 位置 | 问题 | 修复 |
|---|---|---|
| `bsp_log.c` | `xSemaphoreTake` 超时后不检查返回值，循环顶部 `osMessageQueueGet` 会覆盖 DMA 仍在读的 `tx_buf` → 乱码 | 超时时 `HAL_UART_AbortTransmit` 确保 DMA 松开缓冲，再清掉迟到的信号量 |
| `PID.c` | `PID_Trapezoid_Intergral` 写成 `ITerm = ...` 而非 `+=`，**积分作用完全失效**且覆盖前面的累加 | 重构为「先算增量(梯形/矩形二选一)、再统一累加」 |
| `PID.c` | `CoefA` 为 0 时除零 → NaN 顺着 Output 污染整条控制链 | 加 `CoefA > 0` 判断，未配置时按不积分处理 |
| `PID.c` | `dt = 0.01f` 硬编码，改任务周期会静默错掉 Ki/Kd 增益 | 补强警告注释（当前与 `DJI_MOTOR_TASK_PERIOD=10ms` 一致，正确） |
| `bsp_can.c` | `CANRegister`/`CANSetDLC` 注册冲突时 `while(1){}`，卡死开机且日志走队列根本发不出来 | 改为 LOGERROR + 返回 NULL / 保持原 DLC |
| `dji_motor.c` | 3 处 `while(1) LOGERROR(...)` 无限刷日志 | `MotorSenderGrouping` 改为返回 `int`，`DJIMotorInit` 检查并 `vPortFree` 后返回 NULL |
| `Emm_V5.c` | 13 处 `HAL_UART_Transmit_DMA` 返回值全部未检查，失败时命令静默丢失 | 收敛为 `Emm_V5_SendCmd()` 统一检查返回值 |
| `bsp_dwt.c` | `CYCCNT_RountCount * UINT32_MAX` 应为 `* 2^32`，每次溢出少算 1 cycle | 改为 `((uint64_t)UINT32_MAX + 1ULL)` |
| `bsp_dwt.c` | `DWT_Delay(>25.6s)` 时 `wait * CPU_FREQ_Hz` 超 2^32，条件恒真 → **死循环卡死整机** | 抽出 `DWT_DelayCycles()`，上限夹到 `0xF0000000`(≈23.9s) |
| `ADRC.c` | `z03 / b0` 与 `powf(delta, ...)` 未防除零 → NaN（NaN 参与限幅比较全为假，会原样传到电机） | `b0 == 0` 时不做扰动补偿；`delta <= 0` 时 `fal` 退化为线性 |
| `servo.c` | `va_start` 后提前 return 漏 `va_end`（未定义行为） | 补 `va_end` |
| `servo.c` | 7 处 `HAL_UART_Transmit(..., 0xFFFF)` = 65 秒超时，舵机板掉线会卡死任务一分多钟 | 统一改为 `SERVO_TX_TIMEOUT_MS = 300`（9600 波特下最长帧约 108ms） |
| `lift.c` | 6 处中文 printf，ARMCC V5.06 报 `#870-D invalid multibyte character sequence` | 全部改英文 |
| `k230_competition.py` | `postprocess` 异常时每帧最多打印 2100 行，拖垮串口和帧率 | 只打印前 3 条 + 汇总计数 |
| `app_init.c` | `MotorAutoTask` 定义了但创建代码被 `#if 0`，产生 unused 警告 | 函数体一并纳入 `#if 0`，保留动作参数备查 |
| `Emm_V5_CAN.c` / `ZDT_X42_V2.c` | `encoder_zero_offset` 只写不读（`#550-D`） | 补 `*_Get_Encoder_Zero()` 读取接口 |
| `ADRC.{c,h}` / `Emm_V5_CAN.c` | 文件末尾缺换行（`#1-D`） | 补换行 |

### 文档修正

- `lift.c` — `Lift_Up`/`Lift_Down` 注释与代码矛盾（注释写「向上=位移减小」，代码是增大）。
  **经确认代码正确、注释错误**，已改写注释并补充「增量叠加在 target 而非 current 上」的说明
- `lift.c` 文件头 — 删掉与 `mech_params.h` 矛盾的「主动轮直径 18cm」硬编码说明
- `K230.h` — 命令码注释的摄像头对应写反了（写成 `2=看豆(cam1), 3=看正面数字(cam2)`），
  已按协议文档和 Python 代码改为 `2=看豆(cam2), 3=看正面数字(cam1)`

---

## 二、未修复 — 需你确认或实测

> 以下问题**已定位但不能凭猜测改**，涉及机械实测或设计意图，改错的代价比不改更高。

### ⚠ U-1 X / Y 轴轮径未标定（影响行程精度，上场前必须确认）

`modules/mech_params.h:68`：

```c
#define MOTOR_X_WHEEL_DIAMETER_CM  1.2f    /* 量实物填 (占位5.73->周长≈18) */
#define MOTOR_Y_WHEEL_DIAMETER_CM  10.0f   /* 量实物填 */
```

注释自己写着原本想要周长 18cm（直径 5.73），现在填的 1.2 → 周长仅 3.77cm。

按 `Motor_XYZ(x_distance=44)` 算：

| 直径 | 周长 | clk 脉冲 | 圈数 |
|---|---|---|---|
| 1.2cm（当前） | 3.77cm | `(44/3.77)×3200 = 37347` | 11.67 |
| 5.73cm（注释暗示） | 18.0cm | `(44/18)×3200 = 7822` | 2.44 |

**差 4.77 倍。** 需要拿卡尺量实物主动轮直径后填入。Y 轴同理。

### ⚠ U-2 Y 轴双电机方向：`!y_dir` 是否为有意的镜像适配

`modules/motor/motor.c:158`：

```c
Emm_V5_CAN_Pos_Control(MOTOR_Y_ADDR_1,  y_dir, vel_y, acc_y, clk_y, false, true);
Emm_V5_CAN_Pos_Control(MOTOR_Y_ADDR_2, !y_dir, vel_y, acc_y, clk_y, false, true);
```

Y2 用了取反方向，但 `motor.h:63` 和 README 都写「Y轴双电机同向」。
**如果两台电机是镜像安装，代码正确、文档要改；如果同向安装，代码会让 Y 轴扭轴。**
需你确认实际安装方式 —— 这个搞反会直接损坏机构，不敢代改。

### ⚠ U-3 `Motor_XYZ` 的到位等待整段被注释掉

`modules/motor/motor.c:166-218` 的轮询到位循环全被注释，函数签名是 `void`，
而 `motor.h:69` 文档写 `@return 0=全轴到位, -1=超时/堵转`，README 也写「阻塞」。

实际行为：**发完命令立即返回，完全非阻塞**，堵转/超时全检测不到，
调用方靠 `osDelay(6000)` 这类硬编码延时凑合。

代码还在，取消注释 + 改返回值即可恢复。但恢复后：
- 依赖 `Emm_V5_CAN_Read_Flag`（P1-6 已修，现在能读对电机了）
- 会让 `Motor_XYZ` 真正变阻塞，所有调用点的时序都会变

**是否恢复由你决定** —— 涉及整条动作序列的节奏，需要配合实机调试。

### ⚠ U-4 大疆电机失联后无恢复路径

`dji_motor.c::DJIMotorLostCallback` 在失联时 `DJIMotorStop` 置 `MOTOR_STOP`，
重新连上后 `DaemonReload` 会重置计数，但 `stop_flag` 保持 `MOTOR_STOP`，
**电机不会自动恢复，只能重启**。

如果这是有意的安全设计（失联后人工确认再恢复），建议在文档写明；
如果希望自动恢复，需要在 `DecodeDJIMotor` 里加恢复逻辑。**需你定夺**。

### ⚠ U-5 `LIFT_MAX_DISPLACEMENT = 400.0f`（4 米）

`lift.h:45` 的行程限幅对起重机来说等于没有。建议按实际最大行程收紧，
但具体数值需实测。

### ⚠ U-6 `dji_motor.c` 分组只支持 CAN1

`MotorSenderGrouping` 的 `motor_grouping` 只用 0/1/2（CAN1 的三组），
完全没根据 `config->can_handle` 选 CAN2 的组（3/4/5），注释也写着「只使用CAN1」。
当前 lift 用 hcan1 没问题，**但把大疆电机挂到 CAN2 会往 CAN1 发**。
需要时再改（涉及分组逻辑重写）。

---

## 三、已知但保留现状（附理由）

| 项 | 理由 |
|---|---|
| `cmd_register.c` 整套机制无人调用 | `CMD_Dispatch` 全工程无消费者，`CommandTask` 不存在，`lift.c` 注册的 10 条命令永远触发不了。是死代码，但删除影响面大，且 USART1 中断修好后接一个 CommandTask 就能激活，建议保留待启用 |
| `ZDT_X42_V2.c` 无人引用 | 全工程无外部引用，纯占 flash。但它是 Emm_V5 的前代驱动，可能作参考保留 |
| `K230.c` 的 `0x03` 帧解析分支 | 协议里 K230 从不发 0x03（正面数字仅其内部使用），是死分支。留着不影响功能，作防御性代码 |
| `main.c` 的 `"UART1-OK"` 和 RST CSR 诊断块 | 注释写着「定位后删」，但**恰好是验证本次 USART1 修复的最直接手段**，且 RST CSR 能判断复位来源（对曾有反复重启问题的系统很有价值）。建议验证完再删 |
| `Emm_V5.c` 的 `HAL_UART_AbortTransmit` | 原本是绕过 UART5 中断未使能的 workaround。中断已补上，但保留作为上一帧异常未完成时的兜底 |

---

## 四、上电验证步骤

按顺序验证，每步都是下一步的前提：

1. **验证 USART1 恢复**（P0-1）
   上电看 PA9，应先出 `UART1-OK` + `RST CSR=...`，
   随后 action 任务的 `=== K230 Competition (Master) ===`、`[P1] Send LOOK_BEAN` 等**连续不断**。
   > 修复前的症状是：只出前两行（调度器启动前发的），之后一个字都没有。

2. **验证 K230 收帧**（P0-2）
   调试器观察 `k230_rx_frames` 应随通讯稳定增长；
   `k230_rx_badxor` / `k230_rx_badlen` / `k230_rx_rearm_err` 应保持 0 或极小。
   拔插一次 K230 的 TX 线制造错误，确认拔回后通讯能**自动恢复**（这是 ErrorCallback 的作用）。

3. **验证三阶段协议**（P0-3/4/5）
   K230 端 IDE 串口应能看到 `Phase 1/2/3` 全流程；
   若出现 `[WARN]` 说明走了降级路径，记录下来分析识别率。

4. **标定轮径**（U-1）
   量实物填 `mech_params.h`，发 `Motor_XYZ(x=10cm)` 用卷尺量实际行程校验。

5. **确认 Y 轴方向**（U-2）
   低速小行程试运行，观察两台 Y 电机是否同向推进、机构有无扭曲。

---

## 五、改动文件清单

```
Core/Src/usart.c              NVIC 使能 ×3
Core/Src/stm32f4xx_it.c       USART1/USART3/UART5 IRQHandler
cantest.ioc                   NVIC 配置 ×3
bsp/uart/uart_callback.c/.h   事件+错误恢复回调分发, HAL_UART_ErrorCallback
bsp/log/bsp_log.c             DMA 缓冲竞态
bsp/can/bsp_can.c             去 while(1)
bsp/dwt/bsp_dwt.c             溢出常数 + 忙等死循环上限
modules/k230/K230.c/.h        IDLE 切帧 + ACK 计数器 + 返回值 + 诊断计数
modules/action/action.c       走日志系统 + flag 时序 + 重试重同步 + 失败上报
modules/lift/lift.c           中文 printf 改英文 + 注释修正
modules/PID/PID.c             梯形积分 + 除零
modules/ADRC/ADRC.c/.h        除零 ×2 + 行尾换行
modules/dji_motor/dji_motor.c 去 while(1), 分组函数改返回错误码
modules/servo/servo.c         va_end + 超时
modules/ZDT_EmmV5/Emm_V5.c    发送返回值检查
modules/ZDT_EmmV5/CAN/Emm_V5_CAN.c  实例查找加扩展帧过滤 + 零点读取接口
modules/ZDT_X42/ZDT_X42_V2.c  零点读取接口
app/app_init.c                MotorAutoTask 纳入 #if 0
view/k230_competition.py      收帧重构 + 推理兜底 + 识别超时 + 异常限流
```
